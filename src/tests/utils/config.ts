// Setting the default viewport for visual regression tests.
// This is used in the playwright.config.ts file and in
// the runVisualSuite.ts utility function.

export const DEFAULT_VIEWPORT = {
  width: 1280,
  height: 800,
};

export const NARROW_VIEWPORT = {
  width: 393,
  height: 800,
};
